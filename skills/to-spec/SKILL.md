---
name: to-spec
description: Turn the current conversation context into a PRD and DESIGN (doc). Use when user wants to create a PRD and DESIGN (doc) from the current context.
---

This skill takes the current conversation context and codebase understanding and produces a PRD and a DESIGN (doc). Do NOT interview the user — just synthesize what you already know.

